# Rust 迁移记录

2026-09-26，Refs #762。按维护者要求，将本切片的工具和校验测试迁移为 Rust。
未来非 Rust 代码入库的语言选型立项规则由 #765 / #766 独立交付。

## 变更范围

删除当前树中的 `prepare.py`、`run.py`、`analyze.py`、`detail.py`、`test_analyze.py`，
以 workspace 包 `laneflow-post-p3-research` 提供 prepare/run/analyze/verify 子命令。
准备补丁、证据解析、统计、负向测试及进程采集均由 Rust 实现，不调用 Python 包装层。

历史提交 `40392a23`、`4284ce3c` 和迁移前交付 `ddfbb2d2` 保留原始工具，供测量来源复核。
原始记录中的源码提交、运行时间、二进制摘要和测量数字不改写为迁移后的身份。

## 验证与边界

- Rust 从历史原始包严格复算 12 次首轮运行与 3 次 detail 运行，均通过已发布索引校验。
  浮点统计比较不使用误差容忍，文件清单、摘要、运行身份和交通语义也参与核验。
- Rust 分别重建 plain、stages、detail 三份测量源码，各自 916 个文件的 SHA-256
  与历史对应源码索引完全一致。
- 迁移原 8 组负向测试，并增加路径、已发布值篡改及失败进程证据检查。
  采集器已编译并测试失败进程边界；迁移期间未重新执行完整 12+3 性能采集矩阵。
- 新写 JSON 使用 UTF-8/LF。已发布证据的字节保持不变，`.gitattributes` 禁止对证据 JSON
  转换换行，因此 detail 对历史首轮文件的 SHA-256 绑定仍指向同一原始文件。

语言迁移没有产生新的性能结论，也不改变 [研究结果](results.md) 中选择 #763 的依据。
#763 仍需独立实现、正确性验证和适度净收益复测；当前细分时间不等于可实现的净节省。
