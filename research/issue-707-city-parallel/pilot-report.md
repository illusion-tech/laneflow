# #707 第二切片 pilot 报告（WP B smoke + WP C 带暖机 pilot）

> 口径警示（全文适用）：以下分位数全部来自 `diagnostics.json`，**含 1024
> 暖机拍**、probe 不产正式 `measurements.toml`，是**诊断口径**——禁止表述为
> 「观察期正式 Core p95」。正式预算判定（10k Core p95≤2ms @16ms 步长；
> 100k Core p95≤16ms @33ms 步长）只在 WP D 的 `--performance` 样本上做。
> 样本数组已排序、非逐 tick 时序，不做慢拍/Active 下标关联。

## WP B smoke（probe 256 拍，无暖机）

| 规模 | 臂  | 墙钟 | 退出码 | 末拍 Active/Parked/Completed          |
| ---- | --- | ---- | ------ | ------------------------------------- |
| 10k  | w1  | 6 s  | 0      | 7312 / 2520 / 168（合计 10000）       |
| 10k  | w4  | 5 s  | 0      | 同上（逐拍一致，compare probe-match） |
| 100k | w1  | 62 s | 0      | 70761 / 25200 / 4039（合计 100000）   |
| 100k | w4  | 54 s | 0      | 同上（compare probe-match）           |

逐臂核查（10k 与 100k 均满足）：workers 记录 1/4；执行编号不同；二进制
摘要相同；计划摘要相同（plan_digest 一致）；完成拍数 256/256 与计划一致；
比较器 `urban-comparison-v2` 验证通过；Active 实况与场景相符（未把标称
个体数当 Active）。

## WP C pilot（warm-up 1024 + probe 4096 = 5120 拍）

| 规模 | 臂  | 墙钟   | 退出码 | 诊断 p50/p95/p99（ms，含暖机） | 末拍 Active/Parked/Completed |
| ---- | --- | ------ | ------ | ------------------------------ | ---------------------------- |
| 10k  | w1  | 98 s   | 0      | 10.31 / 12.69 / 14.13 ms       | 5887 / 2517 / 1596           |
| 10k  | w4  | 88 s   | 0      | 8.27 / 9.77 / 10.39 ms         | 同上（compare probe-match）  |
| 100k | w1  | 1086 s | 0      | 104.8 / 139.4 / 145.0 ms       | 57976 / 25124 / 16900        |
| 100k | w4  | 824 s  | 0      | 67.9 / 80.0 / 95.6 ms          | 同上（compare probe-match）  |

两组 compare 均 `probe-match`：1w 与 4w 语义逐拍一致（计划摘要、逐拍日志、
检查点、角色见证、计数全等），仅执行封套（diagnostics/measurements）按定义
不同。两臂 worker 记录分别为 1/4（`ComparedRun.workers`）。

## 诊断观察（非正式结论）

- 诊断口径下 w4 相对 w1：10k 整步 p50 −20%、p95 −23%；**100k 整步 p50
  −35%、p95 −43%**（墙钟 −24%）。城市规模（≥1024 Active）出现并行净收益
  的正信号，与 #706 小工作量场景（空区间候选）的观察相反。
- 100k w1 诊断 p95 ≈ 139ms 远超 33ms 步长——**这是含暖机、且为整步墙钟
  的诊断口径**，不能据此判定预算失败；正式判定必须等 WP D 观察窗样本
  （暖机后、剔除观测开销口径）。

## 基线清单结论

| 臂                       | 状态                                                            |
| ------------------------ | --------------------------------------------------------------- |
| P1（4de40e04, w1）       | 实测（上表）                                                    |
| Pp（4de40e04, w4）       | 实测（上表）                                                    |
| B-P2（b99a282e, w4）     | 未测：该提交 harness 无 `--workers` 入口                        |
| R0（P2 前串行）          | 待定：构建窗口已过 + 旧载荷版本被 compare 拒绝（不放宽红线）    |
| R1（算法优化后、并行前） | 待定：提交历史无法干净分离该边界（详见 baseline-manifest.toml） |

## 不能得出的结论

- 不能得出任何正式性能/预算结论（无 `--performance` 观察窗样本）。
- 不能用诊断分位数做慢拍归因（样本排序、无逐 tick 对应）。
- 不能跨提交比较计时（R0/R1/B-P2 未测，且跨版本 compare 被拒）。
