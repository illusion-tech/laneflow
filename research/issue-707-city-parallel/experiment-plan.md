# #707 实验计划（第二切片：入口验证与 pilot；正式窗口待 WP D 执行）

## 运行矩阵

> **范围声明（复审口径）**：本切片交付的是**同源码 1w/4w 初步正式对照**；
> #707 认证所需的 1/2/4/8/16 worker 扫描与 R0/R1/P1/Pp 分解是**保留义务**，
> 排在本矩阵之后的独立批次，本 PR **不关闭 #707**。稳定 Active 对照是
> WP E 独立工作负载（补员规则、拒绝处理、目标范围另冻结），不用
> Mixed 衰减轨迹冒充——本矩阵两行均为 CN-URBAN 衰减轨迹，结论表述
> 受限于此。阶段归因（串行段、实际并发、最长块、回退原因、scratch
> 容量等）在**独立冻结的诊断运行**中采集，正式 latency 运行保持不插桩；
> 两类口径不混合。

| 工作包           | 规模 | 窗口                          | 臂      | 轮次                                                     |
| ---------------- | ---- | ----------------------------- | ------- | -------------------------------------------------------- |
| B smoke          | 10k  | probe 256 拍                  | w1 / w4 | 各 1                                                     |
| B smoke          | 100k | probe 256 拍                  | w1 / w4 | 各 1                                                     |
| C pilot          | 10k  | warm-up 1024 + probe 4096     | w1 / w4 | 各 1                                                     |
| C pilot          | 100k | warm-up 1024 + probe 4096     | w1 / w4 | 各 1                                                     |
| D 正式（未执行） | 10k  | `--performance`（见窗口公式） | 1w/4w   | 每臂同 worker 三轮，交错 r1: 1w→4w, r2: 4w→1w, r3: 1w→4w |
| D 正式（未执行） | 100k | 同上                          | 1w/4w   | 同上                                                     |

## 正式窗口公式（以 plan 生成为准）

- K = 最长信号周期 / Δt；warm-up = max(4K, 512)；observe = max(8K, 4096)。
- 10k 预期 30624 / 61248（Δt=16ms）；100k 预期 14848 / 29696（Δt=33ms）。
- 预期值来自需求计划与信号参数，实际以 `--performance` 计划文件为准。

## WP D 每轮记录清单

每轮正式运行除运行包本身外，登记：运行起止时间（本地时区）、AC/电池
状态、电源方案（powercfg）、其他负载确认（无并发编译/benchmark 的
独占检查，含后台任务盘点）。批次级电源/固件来源见
baseline-manifest.toml [hardware]。

## 停止条件

- 任一拍 step 返回错误或世界失效 → 保留现场目录 + 退出码，停止扩大测量。
- compare 拒绝（语义不一致/来源不匹配）→ 停止后续臂，先归因。
- 仅性能回退（语义全等但变慢）→ 不停止，结果进归因。
- 机器出现其他编译/benchmark 负载 → 当前轮作废重跑（本轮 r 新目录）。

## 口径纪律

- 正式口径：仅 `--performance` 运行产出的 `measurements.toml` 观察窗样本
  （p50/p95/p99/max；`urban-performance-measurements-v3`）。
- 诊断口径：probe/pilot 的 `diagnostics.json` 分位数**含暖机拍**，只作
  诊断；禁止表述为「观察期正式 Core p95」。
- `command_samples_ns` 等样本数组已排序、非逐 tick 时序；禁止用数组下标
  做慢拍/Active 关联分析。
- 预算表：10k（Δt=16ms）Core p95 ≤ 2ms；100k（Δt=33ms）Core p95 ≤ 16ms。
  判定只在 WP D 正式样本上做。

## 基线角色（详见 baseline-manifest.toml）

| 臂   | 定义                             | 状态                                                                                                                                                                |
| ---- | -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1   | 4de40e04、worker=1               | 本切片实测（pilot 诊断口径）                                                                                                                                        |
| Pp   | 4de40e04、worker=4               | 本切片实测（pilot 诊断口径）                                                                                                                                        |
| B-P2 | b99a282e 仅 P2 并行（worker=4）  | 未测：该提交 harness 无 `--workers` 执行配置入口，无法以当前协议跑 w4；混合 harness/runtime 版本会引入额外变量                                                      |
| R0   | 历史串行基线（P2 之前）          | 待定：构建窗口已过（benchmark 前完成全部构建）；且 R0 时代 result 载荷版本低于 v5，4de40e04 的 compare 拒绝跨版本比较，不满足「不放宽 compare_runs」红线            |
| R1   | 局部算法优化后、并行执行组织之前 | 待定：提交历史上无法干净分离「局部算法优化」与「P2 并行组织」的边界（P2 首个 feat 4c1f5714 之前的提交序列未呈现独立的算法优化切片）；同受 R0 的构建窗口与跨版本约束 |
