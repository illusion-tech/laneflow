# LFCA full-spatial 夹具

当前合成编制发射结果。对象前导 `formatVersion = 5`，交通长度/速度为毫米。
`networkRevisionDerivationVersion` 仍为 1。既有 23 个实体种类均有非空样本；
策略表由格式登记提供空逻辑表，策略来源编制及其非空样本由 #564 补齐。

读器拒绝 `formatVersion != 5`。这组样本不证明完整路权策略能力已交付。
