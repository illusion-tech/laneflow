[package]
name = "laneflow-core-test-support"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license.workspace = true
description = "LaneFlow Core 集成测试与 Criterion 基准共享的场景与研究夹具。"
publish = false

[lib]
name = "laneflow_core_test_support"
path = "src/lib.rs"

[dependencies]
laneflow-core = { version = "0.0.0", path = "../laneflow-core" }

[lints]
workspace = true
