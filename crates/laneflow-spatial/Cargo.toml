[package]
name = "laneflow-spatial"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license.workspace = true
description = "LaneFlow 引擎无关的标准空间类型与共享根位姿采样 crate。"
publish = false

[lib]
name = "laneflow_spatial"
path = "src/lib.rs"

[dependencies]
laneflow-static-contract = { version = "0.0.0", path = "../laneflow-static-contract" }
laneflow-static-network = { version = "0.0.0", path = "../laneflow-static-network" }

[dev-dependencies]
laneflow-format = { version = "0.0.0", path = "../laneflow-format" }

[lints]
workspace = true
