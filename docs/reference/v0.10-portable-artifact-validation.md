# #298 可移植规范制品 G1/G2 验证计划

> **状态**：G1 Draft 验证计划；尚无 G1 Pass、G2 实现或产品通过证据<br>
> **计划日期**：2026-08-14<br>
> **设计输入**：`../design/portable-canonical-artifact.md`<br>
> **当前实现基线**：`main@6486c8648837d37cf064745e5b5a7477d842d8af`

本文保存 #298 设计候选的可重放验证计划和未来 G2 证据槽位。动态 Gate、PR 当前头、
外部审阅、CI、Project 和依赖状态以 GitHub 为准；本文不能替代 Gate Ledger。

## 1. 当前事实与禁止误读

- `laneflow-compiler::CompilationOutput` 已原子拥有配对的
  `ValidatedCanonicalLir` 和 `ValidatedSourceMapInput`；
- 当前 workspace 没有 `laneflow-format`，也没有 LFCA/LFSM/LFSD/LFCP 生产实现；
- 当前 `CompilationMetrics::semantic_fingerprint` 只服务同版本重复编译和性能证据，
  不是 artifact digest 或 `NetworkRevisionId`；
- #292/#296 的成功 CI 证明前置编译器和 production source 已交付，不证明 #298 格式；
- 本计划中的“预期”“必须”不是已执行结果。G2 后只能把实际命令、提交、原始输出和
  digest 填入结果列，不能把计划文本改写成追认式通过。

## 2. G1 规范闭合审阅

| ID      | 审阅对象            | 必须证明                                                          | 当前状态                          |
| ------- | ------------------- | ----------------------------------------------------------------- | --------------------------------- |
| `G1-01` | 包边界              | format 不依赖 compiler semantic、Runtime 或 Spatial               | 设计候选；待外部审阅              |
| `G1-02` | 对象前导/目录       | 图/where/offset 一致；closed sections、无 gap/overlap/padding     | 设计候选                          |
| `G1-03` | 基本类型            | endian、宽度、UTF-8、float、signed zero、digest/ID bytes          | 设计候选                          |
| `G1-04` | 表/字段 registry    | 全部 kind/tag/type/required/order/closed enum                     | **阻断：附录 A 未完成**           |
| `G1-05` | 路网修订            | exact semantic payload、domain separation、known revision vectors | **阻断：expected vectors 未冻结** |
| `G1-06` | 来源绑定            | 同次编译、artifact digest/length、compiler/source provenance      | 设计候选                          |
| `G1-07` | 差异绑定            | genesis/base/target、digest/length、排序和迁移非权威              | 设计候选                          |
| `G1-08` | 发布描述符/信任交接 | artifact/source map/receipt exact binding；descriptor 不自证      | 设计候选；待 #299 接口审阅        |
| `G1-09` | 原子发布            | staging、immutable objects、单提交点、rollback                    | 设计候选                          |
| `G1-10` | 受限读取            | pre-hash limits、checked arithmetic、稳定错误分类                 | **阻断：数值上限未冻结**          |
| `G1-11` | 编码方案            | 独立实现可重建；依赖/安全/维护成本                                | 候选选择；待 oracle spike         |
| `G1-12` | 治理                | Related PR、精确头外部 clean review、metadata、G1 marker          | 待当前设计 PR                     |

## 3. G2 功能与负向测试矩阵

### 3.1 可移植规范制品

| ID        | 输入/扰动                            | 预期                                                           |
| --------- | ------------------------------------ | -------------------------------------------------------------- |
| `ART-001` | 最小合法 headless LIR                | 产生 `LFCA-V1-MIN-HEADLESS` exact bytes/digest/length/revision |
| `ART-002` | 完整空间 LIR                         | 全部 22 种实体、关系、规则、空间和约束进入 closed tables       |
| `ART-003` | 声明、集合与 hash iteration 重排     | artifact exact bytes 相同                                      |
| `ART-004` | 只改变来源位置/provenance            | revision 相同；artifact digest 和 LFSM digest 改变             |
| `ART-005` | 支持 worker 数矩阵                   | artifact/LFSM/LFSD exact bytes 均相同                          |
| `ART-006` | 合法输入包含 `-0.0`                  | 编译边界输出 `+0.0` bits                                       |
| `ART-007` | wire 注入 NaN/Infinity/negative zero | 结构预检稳定失败                                               |
| `ART-008` | 未知/重复/乱序 section/table/field   | 失败关闭，不建立 checked view                                  |
| `ART-009` | offset gap/overlap/overflow/尾字节   | 失败关闭，不越界读取或超限分配                                 |
| `ART-010` | revision 声明被篡改                  | #299 独立重算失败；format view 不把声明提升为可信              |

### 3.2 源映射和语义差异

| ID         | 输入/扰动                               | 预期                                                      |
| ---------- | --------------------------------------- | --------------------------------------------------------- |
| `MAP-001`  | 多模块、多来源文档、派生关系            | LFSM 记录完整且排序确定                                   |
| `MAP-002`  | artifact digest/length/revision 错配    | `SourceMapArtifactMismatch`，不得逐行定位后再补做 binding |
| `MAP-003`  | 来源文档键/范围越界或重复               | 失败关闭                                                  |
| `DIFF-001` | 无基线                                  | 规范 Genesis binding，所有实体/关系按新增报告             |
| `DIFF-002` | 相同 base/target                        | 空变化集合但 binding 完整                                 |
| `DIFF-003` | add/remove/reconnect/geometry/rule      | 分类、before/after 与排序符合 registry                    |
| `DIFF-004` | 重复 relation value                     | 以最低 before/after localIndex 确定性破同值               |
| `DIFF-005` | base/target digest/length/revision 错配 | 失败关闭；diff 不授予迁移                                 |
| `DIFF-006` | 仅 target image layout/profile 变化     | LFSD 不产生伪语义记录                                     |

### 3.3 长度、损坏、上限和原子性

| ID         | 输入/故障点                         | 预期                                       |
| ---------- | ----------------------------------- | ------------------------------------------ |
| `SEC-001`  | 已知 transport 长度超过上限         | hash/read 前 O(1) 拒绝                     |
| `SEC-002`  | 未知流超过上限                      | 只读取到 `max + 1` 后拒绝                  |
| `SEC-003`  | count/stride/offset 算术溢出        | checked failure；不分配、不 panic          |
| `SEC-004`  | 每个 byte boundary 截断             | 稳定 truncated/length mismatch             |
| `SEC-005`  | 每个 section 单 bit 损坏            | descriptor digest 或语义验证失败           |
| `ATOM-001` | artifact 暂存写失败                 | 无候选成功、无发布提交点                   |
| `ATOM-002` | LFSM/LFSD 写、flush、close 失败     | 无可拆分 artifact 成功、无发布提交点       |
| `ATOM-003` | immutable object 已存在且不同 bytes | collision/mismatch；禁止覆盖               |
| `ATOM-004` | receipt/LFCP/manifest 提交失败      | 已写对象保持未引用，不形成部分发布         |
| `ATOM-005` | 全部成功                            | 只有认证 manifest/指针单提交点后可称已发布 |

## 4. Known vectors 与独立 oracle

每个向量目录必须保存：

```text
input/                         # 非 emitter 生成的冻结语义输入
expected.lfca
expected.lfsm
expected.lfsd
expected.lfcp                 # #299 receipt fixture 可用后
expected.json                 # digest、length、revision、版本和适用范围
README.md                     # 人工可读字段/offset 推导
```

至少三套生产者/检查者角色：

1. `laneflow-compiler` production emitter；
2. 不调用 emitter 的独立 Rust oracle/validator；
3. 只依据本文规范的非 Rust 或独立脚本 oracle。

expected bytes 作为仓库固定文件进入版本控制；测试不得先调用 production emitter 写出
expected 再与自身比较。更新任何 expected bytes 必须在 PR 中展示语义差异、版本决策和
旧向量处置，不能静默刷新 snapshot。

G1/G2 审阅还必须逐结构核对 ASCII 图、图后 `where:` 字段定义、offset/constraint 表与
known-vector byte offsets。图中的 32-bit wire slot 不得被实现误读为网络字节序；所有
多字节整数继续按设计文档明确的小端序生成。任何字段名、短名、宽度、顺序、重复次数、
变长边界或 little-endian 解释不一致都阻断 G1/G2，不能以“图仅供参考”豁免。

## 5. 跨平台与确定性执行矩阵

| 轴         | 最小集合                                                                     |
| ---------- | ---------------------------------------------------------------------------- |
| OS/target  | Windows `x86_64-pc-windows-msvc`；Ubuntu `x86_64-unknown-linux-gnu`          |
| process    | clean process；同进程重复；不同 hash seed/分配地址                           |
| workers    | single-thread；每个正式支持的 worker 数                                      |
| workload   | min headless；full spatial；P100 正式最高级；硬上限附近                      |
| comparison | artifact、source map、semantic diff exact bytes + digest + length + revision |

共享 CI 必须上传或核对同一已知摘要，不接受“各平台测试都绿但没有交叉比较 exact bytes”。
若某平台尚非 LaneFlow 正式支持目标，只能作为附加观察，不能用它替代 Windows 本机和当前
Ubuntu required CI 的双轴证据。

## 6. 性能与资源证据

G2 在 #292 的合法 P100 production workload 上至少记录：

- artifact/LFSM/LFSD 各自和总输出 exact bytes；
- emitter wall-clock、hash wall-clock 与 end-to-end compiler + emitter wall-clock；
- 暂存容量、compiler-controlled peak、失败后的 retained bytes；
- single-thread 与支持 worker 数的结果；
- min、正式五级最高级、硬上限附近输入；
- Windows 固定机原始轮次和共享 CI correctness/determinism。

具体门槛和硬上限必须在 G1 根据 spike 证据冻结。未冻结前不得事后选择恰好让实现通过的
阈值，也不得把 #292 内部 LIR 的 `output_logical_bytes` 当作 LFCA 文件大小。

## 7. 未来结果登记表

| 项目                        | source commit | 命令/制品 | 结果   |
| --------------------------- | ------------- | --------- | ------ |
| G1 independent oracle spike | pending       | pending   | 未执行 |
| G1 exact vectors            | pending       | pending   | 未冻结 |
| Workspace correctness       | pending       | pending   | 未执行 |
| Cross-platform exact bytes  | pending       | pending   | 未执行 |
| Corruption/limits           | pending       | pending   | 未执行 |
| Atomic publication faults   | pending       | pending   | 未执行 |
| P100 performance/resources  | pending       | pending   | 未执行 |

只有实际执行后才能追加结果；保留失败结果和修正提交的可追溯关系。
