# #292 编译器基础设施 G2 实现验证基线

> **状态**：G2 本地实现与生产 R0 验证完成；等待 Delivery PR G3<br>
> **验证日期**：2026-08-04<br>
> **实现基线**：`main@f482b1bd83a0a7abf52db93e5ea3ced052485fc1`<br>
> **G2 正确性切片末端**：`11fe0b89b9dfa0b05f02577a9d4ae200e5c7af3a`<br>
> **生产基线扩展**：与本证据同一提交

> **2026-08-10 维护说明**：本文是 #292 当时的验证快照。随后 #297 取消 current JSON
> 编译器接入，`laneflow-compiler-test-support` 已删除 `laneflow-data`/`serde_json`
> 开发依赖与 `current_equivalence` 测试转换器，改由 `projection_equivalence` 的编译器
> 原生夹具验证投影；下列历史命令和结果不再是当前可重放入口。

本文保存 #292 首个生产纵向切片的本地实现、迁移等价、依赖方向和验证事实。GitHub
Issue、Delivery PR、外部审阅、CI、G3/G4 评论和 Project 状态继续由动态治理记录负责；
本文不能替代这些闸口（Gate）。

## 1. 实现范围

本轮建立三个边界明确的包：

- `laneflow-static-contract`：`no_std` 静态契约值类型、Identity v1 实体/字段登记表、
  `StableId128`、有类型稳定标识和致密逻辑序号；
- `laneflow-compiler`：`Synthetic DSL → typed AST → HIR → MIR → validated Canonical LIR`
  干净单工作线程编译，结构化诊断、来源伴随数据、资源上限和完整 G1 领域子集；
- `laneflow-compiler-test-support`：只接受 `&ValidatedCanonicalLir` 的集成专用当前态投影
  （integration-only current projection）；该包 `publish = false`，不是生产后端。

G1 首批语义覆盖 LaneGraph、横断面所有者树、路口/Movement/ManeuverPath、StopLine、
ManeuverGate、WaitingZone、固定时制 Signals、Parking、ParticipantClass、AccessRule、
VehicleProfile、StaticRoute、CanonicalFrame 和 LaneEdge 规范空间几何。

## 2. 架构与依赖核对

`cargo tree --depth 1` 的本地事实是：

- 生产编译器（production compiler）`laneflow-compiler` 的依赖只有
  `laneflow-static-contract`、`blake3` 和 `sha2`；没有
  `laneflow-core`、`laneflow-spatial` 或 `laneflow-data`；
- `laneflow-compiler-test-support` 单向依赖编译器 + 当前核心/空间层（current
  Core/Spatial）；`laneflow-data` 与 `serde_json` 只作为迁移测试的开发依赖
  （dev-dependency）；
- 当前外部标识（current external ID）、运行时句柄（runtime handle）和
  `SpatialRegistry` 不进入 Canonical LIR；
- 投影从 LIR 的稳定标识（stable ID）与逻辑序号（ordinal）建立 current 对象，不反向
  调用 current 构造器充当编译器语义验证（compiler semantic validation）。

该依赖方向满足 ADR 0020 与 `compiler-foundation.md` 的分层要求。

## 3. 正确性与确定性证据

生产编译器测试覆盖：

- Identity v1 的全部登记实体已知向量、规范字节、BLAKE3-128、重复与碰撞失败关闭；
- 模块闭包、规范依赖顺序、来源描述符与来源位置；
- 各领域的引用、所有者、拓扑、数值、资源上限、错误优先级和失败原子性；
- 声明重排、集合重排、来源变化和进程随机 `HashMap` 顺序不改变规范语义；
- HIR/MIR/LIR 采用私有有类型 `u32` 区块键，Canonical LIR 使用公开有类型 ordinal。

`LF-COMP-CURRENT-EQUIV-v1` 的两个固定样例另行证明：

- 当前加载器（current loader）与独立测试前端（frontend）产生相同的完整 current 静态
  契约快照；
- 完整走廊 66 条 `LaneEdge` 的起点、中点、终点空间采样完全相同；
- 一千个 20 ms 固定步进中的车辆状态与所有核心事件（Core Event）在外部标识
  （external ID）归一化后逐 tick 相同；
- 使用新 `Compiler` 重编译得到相同稳定标识映射与静态快照；
- 所有 LIR 稳定实体的 `identityFields` 均按 Identity v1 公开 SSOT 独立编码并重新派生
  BLAKE3-128，逐项匹配该行 `stableId`；
- LIR 的固定时制信号周期以及 ParticipantClass 深度/传递子树关系，匹配 current Core
  只从相位持续时间或父类关系独立重建的结果；
- LIR 的路线转换、机动/门/等待区正向出现项及 LaneEdge/ManeuverPath/ManeuverGate/
  WaitingZone 反向出现项，逐字段匹配 current Core 只从路线边序列独立重编译的结果；
- LIR 的 Junction→Movement、Movement→ManeuverPath 和内部边 owner 索引，逐项匹配
  current `JunctionRegistry` 从 parent 引用与路径边序列独立重建的结果；四类反向路线
  出现项的实体范围和连续表也全部进入语义指纹；
- LIR 的横断面 owner/member、机动路径/停止线/信号组控制成员、控制器组/相位、
  等待区及停车区域成员索引，逐项匹配 current registry 从另一方向引用独立重建的结果；
- 编译器在冻结 LIR 前拒绝同一 `LaneEdge` 的重复 `StopLine`；停止线一旦启用入口
  `ManeuverGate`，每个后继转换必须有候选 `ManeuverPath`，每条候选路径必须有
  `transitionIndex=0` 的入口门，与 current `SignalRegistry` 的闭包约束一致；
- LIR 的空间段长、累计弧长、总弧长和局部基，匹配点表独立重建结果与重建后
  `SpatialRegistry` 的段内采样；
- 多机动门等待区保留 3 个机动门出现项（gate occurrence）和 2 个等待区出现项
  （waiting-zone occurrence）。

## 4. 本地验证结果

| 命令                                                                                                                                                                                               | 结果                                                                         |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `cargo +1.96.0 fmt --all -- --check`                                                                                                                                                               | 通过                                                                         |
| `cargo +1.96.0 check --offline --locked --workspace --all-targets --all-features`                                                                                                                  | 通过                                                                         |
| `cargo +1.96.0 test --offline --locked --workspace --all-features`                                                                                                                                 | 通过；具名大规模/测量测试继续按既有约定保持 ignored                          |
| `cargo +1.96.0 test --offline --locked --doc --workspace --all-features`                                                                                                                           | 通过                                                                         |
| `cargo +1.96.0 clippy --offline --locked -p laneflow-static-contract -p laneflow-compiler --all-targets --all-features -- -D warnings`                                                             | 通过                                                                         |
| `cargo +1.96.0 clippy --offline --locked -p laneflow-compiler-test-support --all-targets --all-features --no-deps -- -D warnings`                                                                  | 通过                                                                         |
| `cargo +1.96.0 test --offline --locked --release -p laneflow-compiler -p laneflow-compiler-test-support --all-features`                                                                            | 通过                                                                         |
| `cargo +1.96.0 test --offline --locked --release -p laneflow-compiler-test-support --test current_equivalence p100_production_compiler_baseline -- --ignored --exact --nocapture --test-threads=1` | 通过；五级、35 个正式样本，完整测试 0.41 s                                   |
| `cargo +1.96.0 doc --offline --locked -p laneflow-static-contract -p laneflow-compiler -p laneflow-compiler-test-support --no-deps`                                                                | 通过                                                                         |
| `cargo +1.96.0 deny check`                                                                                                                                                                         | advisories、bans、licenses、sources 均通过；只报告既有允许的重复版本 warning |
| `cargo +1.96.0 run --offline --locked -p xtask -- format-md-tables --check docs .agents`                                                                                                           | 96 个 Markdown 文件通过                                                      |
| `git diff --check`                                                                                                                                                                                 | 通过                                                                         |

严格工作区（workspace）Clippy 仍被 current Core 的既有 `result_large_err`、
`needless_range_loop`、
`collapsible_if` 等告警阻断；本分支没有修改这些位置。新建三个包已按上表分别通过
`-D warnings`，不得为使本切片变绿而顺带改写 current Core API 或错误布局。Windows
构建偶发报告 incremental session 目录“拒绝访问”；命令最终状态与测试结果均成功，
该提示不构成代码失败。

## 5. 性能门槛适用性结论

G2 映射发现 `LF-COMP-P100-R0-v1` 的 126 条研究建议不能按原自然身份直接判定生产
编译器：`LF-COMP-ID-v1` 的抽象实体组合无法构造合法生产机动路径，模块图的抽象
`crossModuleReferences` 也不能在不新增领域声明的情况下映射为 Synthetic DSL；研究
阶段记录与受控分配不是生产 HIR/MIR/LIR 的观测值。精确差额与禁止伪预算规则见
`compiler-foundation.md` 第 11.4 节。

append-only [G1 性能修订][g1-performance-revision] 随后选择真实合法产品场景建立生产
R0，[规模阶梯更正][g1-scale-correction] 又以资源上限失败证据把正式五级冻结为 1–5 份
完整信号化走廊。因此当前事实是：

- `LF-COMP-P100-INITIAL-v1` 资源上限仍有效，并已由生产入口执行失败关闭；
- #308 仍是容量估算与实现选型输入，不因本发现失去研究价值；
- 两个固定样例继续证明迁移正确性；`LF-COMP-P100-PRODUCTION-R0-v1` 另行提供真实
  `Compiler::compile` 的时延、峰值、保留容量、输出量和五级规模扩展证据；
- 35 个正式样本全部成功且同级语义指纹一致；最高级 5 份完整走廊 / 330 条
  `LaneEdge` 的中位时延为 11.3253 ms，含源映射冻结的全管线编译器控制峰值为
  3211621 字节，保留容量为 0；
- 可以勾选 #292 的 compile latency、retained memory 与规模扩展本地验收，但这仍不能
  替代 Delivery PR CI、外部审阅或整体 G3 Pass。

## 6. 剩余 G3 动作

性能设计与本地证据缺口已经关闭。整体 G3 仍须：

1. 推送当前实现并创建唯一 Delivery PR，以 `Closes #292` 建立 Development 关联；
2. 把 #292 的 Delivery PR 与 Project status 更新为当前审阅状态；
3. 等待 required CI，并对 Delivery PR 当前精确头取得有效外部 reviewer completion；
4. 若有 findings，逐项处置、resolve 后再取得当前精确头 clean re-review；
5. 运行 full-set `check-gate-evidence g3`，新增 append-only `## G3 合并判断`。

在上述远端证据完成前，本文状态保持“等待 Delivery PR G3”，不提前声明正式通过。

[g1-performance-revision]: https://github.com/illusion-tech/laneflow/issues/292#issuecomment-5175472658
[g1-scale-correction]: https://github.com/illusion-tech/laneflow/issues/292#issuecomment-5175536346
