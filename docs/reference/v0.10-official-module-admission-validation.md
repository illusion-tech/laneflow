# #315 官方前端共同受检模块接入 G2 验证基线

> **状态**：提交前本地实现验证快照；不声明 G3/G4<br>
> **验证日期**：2026-08-05<br>
> **实现基线**：`main@4465df3`<br>
> **候选分支**：`codex/315-official-module-admission-g2`<br>
> **候选提交**：尚未形成；创建提交后必须把本项替换为不可变完整 SHA，并在该精确头重跑第 3、4 节命令，本文才能成为 G3 候选证据<br>
> **G2 授权**：[#315 G2 设计判断](https://github.com/illusion-tech/laneflow/issues/315#issuecomment-5189685733)

本文保存 #315 官方前端共同受检模块接入切片的本地实现、配置档版本边界、失败关闭和配对性能
事实。GitHub Issue、Delivery PR、外部审阅、CI、G3/G4 评论和 Project 状态继续由动态治理
记录负责；本文不能替代这些闸口（Gate）。

## 1. 实现范围

本轮只实现全部官方前端共享、且已经由 #315 G1/G2 授权的编译器基础：

- `CompilationUnit` 只保留编译器私有的共同 `TypedAstModule` / `TypedAstDeclaration`；
- 各具体官方模块只能通过字段私有包装进入同一条私有、原子准入路径；生产公共面没有
  第三方 `SourceLanguage`、通用 `add_module` 或可伪造的中间能力值；
- 逻辑 `SourceModuleDescriptor` 与一个或多个 `SourceDocumentDescriptor` 分离，模块使用
  版本化 `sourceDocumentSetDigest`，文档各自绑定键、SHA-256、精确来源记录长度、所属
  命名空间和冷显示/审计来源；
- 文档集摘要 v1 对按文档键排序后的紧凑描述符做一次 SHA-256；来源记录在模块构建完成后
  不进入编译单元保留状态；
- `LF-COMP-P100-INITIAL-v1` 保持不可变且只接受每模块一份文档；
  `LF-COMP-P100-INITIAL-v2` 只新增 `max_source_document_count = 1566`，没有默认值、
  自动升级或配置档身份混淆；
- 共同准入在提交前统一检查命名空间、文档键、导入、所有资源维度和控制存续内存；任一
  失败都不改变 builder；
- 共同准入内部以 `AdmissionPlan` 表达“已校验、未提交”事务，以 `AdmissionTotals`
  一次替换全部累计资源状态；文档索引只能从 `PendingSourceDocumentIndex` 消费式冻结为
  `FrozenSourceDocumentIndex`，源映射只取得具名解析结果；
- 共同准入建立并持久保留文档键到“所属规范模块序号 + 紧凑文档序号”的索引；`build`
  原位冻结绑定，来源映射复用它而不再次分配哈希表；
- 每个位置以自身 `SourceSpan.source_document_key` 和真实所属 MIR 模块共同解析；未登记键与
  跨模块错绑均返回 `LF-COMP-SOURCE-DOCUMENT-OWNERSHIP-MISMATCH`，不 panic、不静默误归因；
- 文档索引、平坦源映射文档描述符输出以及源映射 scratch 均进入统一资源预算。

明确不在本轮范围内：

- #296 的几何文档线格式、领域解析、文档角色和专用降阶；
- #297 的当前 ScenarioManifest / Traffic / Spatial 原始输入签名、来源验证、严格导入策略、
  三文档角色和专用降阶；
- 第三方自定义前端扩展点、可移植规范制品、独立验证器或生产路径切换。

因此本轮不能声明 #296 或 #297 已实现，也不能在 #315 G4 前解除它们的 G2 阻塞。

## 2. 正确性、配置档版本边界与失败关闭证据

新增或扩展的测试证明：

- v1 精确字段保持不变；v2 除 `SourceDocumentCount` 外逐维等于 v1；
- v1 多文档模块返回结构化 `LF-COMP-PROFILE-INCOMPATIBLE`，随后可用合法模块重试；
- v2 在 1566 份文档边界成功，1567 份失败，失败后 builder 状态不变；
- 同一候选内部以及既有编译单元之间的重复文档键均失败关闭；
- 共同准入负责的 17 个累计资源维度逐项触发精确维度诊断，且每次失败后模块、命名空间
  索引、文档索引和累计计数均保持空状态；
- 文档输入重排不改变文档集摘要或冻结顺序；单文档已知向量为
  `e23b2cd10cc8f74c1f21280373cfff89163f63006f262f631506ee4fad2fcf7e`；
- 精确三文档样例中，实体声明位置落到第二份文档、关系位置落到第三份文档，第一份文档
  没有实体声明；三份冷来源可区分且构造期来源字节释放后仍可查询；
- 未登记文档键和借用另一逻辑模块文档键的两个反例均产生结构化归属诊断；
- 测试计数器证明三份原始文档各执行且只执行一次 SHA-256；
- 测试专用第二官方模块包装复用同一条共同准入路径；compile-fail doctest 证明外部调用方
  不能构造文档描述符字段，也不能调用通用模块接入；
- 峰值内存差分测试精确覆盖文档键、unit 文档描述符、持久文档索引和源映射平坦文档表；
  1566 文档边界执行完整 `Compiler::compile`，证明不触发隐含 scratch 越界。

## 3. 不兼容迁移说明

本切片有意移除旧的一模块一文档公共查询，不提供 deprecated shim、首文档回退或兼容包装：

- `SourceModuleDescriptor::source_content_digest()` 改为模块级
  `source_document_set_digest()`；两者语义不同，不能机械保留旧名称；
- `SourceModuleDescriptor::source_record_byte_len()` 与 `source_document_key()` 移到
  `SourceDocumentDescriptor`；调用方从 `SyntheticModule::source_documents()` 或
  `ValidatedSourceMapInput::source_documents()` 取得逐文档值；
- 单文档摘要与文档集摘要不要求相等。需要逐文档身份的调用方必须显式遍历文档。

因此未来提交和 Delivery PR 必须以 Conventional Commits 的 `!` 与 `BREAKING CHANGE:`
明确发布该迁移；不得把兼容层重新带回实现。

## 4. 本地验证结果

实际工具链为 `rustc 1.97.1 (8bab26f4f 2026-07-14)` / `cargo 1.97.1`。

| 命令                                                                                                                                        | 结果                                                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| `cargo fmt --all -- --check`                                                                                                                | 通过                                                                                                            |
| `cargo check --locked --workspace --all-targets --all-features`                                                                             | 通过                                                                                                            |
| `cargo test --offline --locked --workspace --all-features`                                                                                  | 通过；完整工作区单测、集成测试与 doctest 全部成功                                                               |
| `cargo clippy --locked -p laneflow-compiler --all-targets --all-features -- -D warnings`                                                    | 通过                                                                                                            |
| `cargo clippy --locked -p laneflow-compiler-test-support --all-targets --all-features --no-deps -- -D warnings`                             | 通过                                                                                                            |
| `cargo test --offline --locked --release -p laneflow-static-contract -p laneflow-compiler -p laneflow-compiler-test-support --all-features` | 通过；编译器 105 个单测中 104 个通过，1 个 admission 性能测试按约定 ignored；正常/compile-fail doctest 全部通过 |
| `cargo doc --offline --locked -p laneflow-static-contract -p laneflow-compiler -p laneflow-compiler-test-support --no-deps`                 | 通过                                                                                                            |
| `cargo deny --format json --offline --locked --all-features check advisories bans licenses sources`                                         | 通过；四类均 0 error，只报告既有允许的重复版本 warning                                                          |
| `cargo run --offline --locked -p xtask -- format-md-tables --check docs .agents`                                                            | 通过；97 个 Markdown 文件                                                                                       |
| `git diff --check`                                                                                                                          | 通过                                                                                                            |

完整工作区测试第一次只达到 120 秒工具超时，没有测试失败；同一 exact-head 命令以更长的
有界超时重跑后退出码为 0。独立离线 `cargo deny` 证明 advisories、bans、licenses、
sources 均为 0 error，只保留既有允许的重复版本 warning。

Windows 构建偶发报告 incremental session 目录“拒绝访问”；相关命令最终状态与测试结果
均成功，该缓存提示不构成代码失败。

## 5. 同机配对性能结果

使用 #292 冻结的真实生产五级工作负载，在同一工作区、同一 Rust 1.97.1 release 单线程
命令下以隔离 detached 工作树测 `main@4465df3`，再连续测量候选两次。每次包含五级、
每级 1 次预热与 7 个正式样本；基线与候选使用同一份已补充 MAD 的基准实现。
所有样本成功，同级语义指纹稳定，最高级仍为 10 个模块、330 条 `LaneEdge`、20975 条
LIR 记录和 600887 逻辑输出字节。

| 样本                | 最高级中位时延 |       MAD | 相对基线 | 编译器控制峰值 | 相对基线 | 保留容量 |
| ------------------- | -------------: | --------: | -------: | -------------: | -------: | -------: |
| `main@4465df3` 基线 |      5.5684 ms | 0.0456 ms |        — |      3211621 B |        — |      0 B |
| 候选第 1 次         |      5.6442 ms | 0.0715 ms |   +1.36% |      2728367 B |  -15.05% |      0 B |
| 候选第 2 次         |      5.4275 ms | 0.0646 ms |   -2.53% |      2728367 B |  -15.05% |      0 B |

候选两次最高级时延一正一负，均落在同轮抖动范围内，没有可重复时延回退。两次峰值均
减少 483254 字节；最终数值已经包含此前漏计的持久文档索引与源映射平坦文档描述符，
内存收益来自模块完成后不再保留完整 `LFSOURCE` 记录，且 HIR/MIR 不再保存模块默认文档
字段。所有级别样本均成功，最高级语义指纹均为
`f8bf90d57c8b7eea9045c17dcb59296fe72ce6601619baddf927ea54812b865d`。

共同准入还使用独立 ignored release 测试，只计时已经完成前端构造的 100 个具体模块
（共 300 份文档）依次 `add_test_official_module` 到 `build`：

| 预热 | 正式样本 |  中位时延 |       MAD | 冷来源字符串 | 结果受控存续 |
| ---: | -------: | --------: | --------: | -----------: | -----------: |
|    1 |        7 | 0.1259 ms | 0.0119 ms |       5500 B |     102816 B |

模块构造、逐文档来源编码和摘要都在停表外；测试同时输出来源字符串字节与结果受控存续，
不会把前端解析成本归给共同接入。逐文档摘要计数由正确性测试独立证明为每文档一次。

内部结构收敛后另以同一 ignored release 测试复测共同准入。重构前即时基线为
`median_ns=134200`、`mad_ns=3100`、`controlled_live_bytes=102816`。最终代码在 release
重建后首次运行受调度扰动得到 `154000 ± 4600 ns`；随后三个独立热态进程依次得到
`132100 ± 10300 ns`、`137400 ± 6800 ns`、`141700 ± 11000 ns`。后三轮离散区间均覆盖
重构前基线，中位数的中位数为 `137400 ns`（相对基线 `+2.38%`），且受控存续逐轮精确
保持 `102816 B`，没有可重复的时延或内存回退。

该结构收敛只新增 crate 私有 Rust 模块、值对象与类型状态（typestate）包装：没有新增 crate、依赖、
公共 trait、前端插件面、deprecated shim 或兼容回退。`module.rs` 继续作为原公共 API 的
薄 façade；来源记录编码和源映射冻结分别下沉到私有实现模块。

该配对只验证 #315 共同准入对现有 Synthetic 生产基线的回归边界；它不包含 #296 几何
解析或 #297 current 原始制品解析成本，也不能替代它们各自的重新资格验证。

## 6. 结论与剩余 G3 动作

#315 当前工作树已满足实现、正确性、失败关闭、配置档版本边界、依赖边界和配对性能要求。
由于尚未形成不可变候选提交，本快照还不能作为精确头 G3 证据。后续仍须：

1. 形成候选提交，推送当前分支并创建唯一 Delivery PR，以 `Closes #315` 建立 Development
   关联；
2. 更新 Issue / PR 的 Project 状态，等待 required CI；
3. 对 Delivery PR 当前精确头取得有效外部 reviewer completion；
4. 若有 findings，逐项处置、resolve，并在当前精确头重新取得 clean re-review；
5. 运行 full-set `check-gate-evidence g3`，新增 append-only `## G3 合并判断`；合入后再执行
   G4 收口并解除 #296/#297 的 G2 阻塞。

在候选提交与上述远端证据完成前，本文只声明提交前本地验证通过，不提前声明 G3/G4。
